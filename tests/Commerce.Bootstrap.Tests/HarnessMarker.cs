namespace Commerce.Bootstrap.Tests;

/// <summary>
/// Marks the point where the `dotnet test Commerce.sln` harness became a real,
/// runnable command (Unit 1 of the commerce-foundation walking skeleton).
/// </summary>
public static class HarnessMarker
{
    public static bool IsScaffolded => true;
}
