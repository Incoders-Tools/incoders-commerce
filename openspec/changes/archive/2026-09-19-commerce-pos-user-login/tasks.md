# Tasks: Commerce POS User Login

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~750 (design's own estimate: Unit 1 ~230, Unit 2 ~220, Unit 3 ~300) |
| Effective review budget (session default) | 1500 changed lines |
| 400-line budget risk (vs. skill default) | Medium (design's own figure, ~2x the 400-line default but comfortably under this session's 1500-line budget) |
| Budget risk vs. the 1500-line session budget | Low |
| Chained PRs recommended | No (delivery strategy is `single-pr`; design's own 3-PR chain recommendation is preserved as in-PR task/commit ordering instead) |
| Suggested split | Single PR, internally ordered so Unit 1 → Unit 2 → Unit 3 land as sequential, individually-green commit groups |
| Delivery strategy | single-pr |
| Chain strategy | none needed (under budget) |

Decision needed before apply: No
Chained PRs recommended: No
400-line budget risk: Medium (vs. skill default) / Low (vs. session's 1500-line budget)

**Dependency ordering preserved as task order, not PR order**: Unit 1 (pure local code — `OperatorPinCredential`, `CachedOperator`, `LocalOperatorStore`, `CurrentOperator`) has nothing calling it yet and MUST land first. Unit 2 (the two new `Device.cs` server routes) is reachable by no shipped client yet and MUST be green before Unit 3. Unit 3 (`OperatorLoginWindow`, `App.xaml.cs`, `MainWindow.xaml.cs`, `PosHostBuilder` wiring) is the only slice that changes observable POS behavior and depends on both prior units. This mirrors the design's Work Unit 1→2→3 dependency exactly.

**No WPF UI test harness in this repo (verified, not assumed)**: a repo-wide search found exactly one existing test referencing `PairingWindow`/`MainWindow` — `PosCompositionRootTests.Build_Resolves_MainWindowDependencies_AsSingletons` in `tests/Commerce.Integration/PosCompositionRootTests.cs` — and it tests DI resolution/singleton lifetime in `PosHostBuilder`, not window code-behind logic. There is no existing convention for unit-testing `PairingWindow.xaml.cs` or `MainWindow.xaml.cs` button-click/window-flow logic itself. Per the design ("manual WPF walkthrough is the only coverage for the window flow itself"), Unit 3's window/UI-wiring tasks below are explicitly manual-verification tasks, not fabricated automated tests. Pure logic extracted to non-WPF classes (`OperatorPinCredential`, `LocalOperatorStore`, `CurrentOperator.ResolveActorId`) remains fully unit-testable and follows strict RED/GREEN like the rest of the repo.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | `OperatorPinCredential` + `CachedOperator` + `LocalOperatorStore` + `CurrentOperator` + unit tests | Single PR, commits 1.x | `dotnet test tests/Commerce.Integration --filter FullyQualifiedName~OperatorPinCredential\|FullyQualifiedName~LocalOperatorStore\|FullyQualifiedName~CurrentOperator` | xUnit, temp directory for DPAPI file round-trip; no network, no WPF | Revert; dead files nothing calls |
| 2 | `POST /device/operators/verify` + `GET /device/operators/{userId}/status` + `OperatorProvisioningClient` + integration tests — depends on Unit 1's `CachedOperator`/verifier shapes only for contract alignment, not runtime coupling | Single PR, commits 2.x | `dotnet test tests/Commerce.Integration --filter FullyQualifiedName~OperatorProvisioning` | `WebApplicationFactory` + live Postgres (two seeded orgs) | Revert; routes unused, no shipped client reaches them yet |
| 3 | `OperatorLoginWindow` + `App.xaml.cs` sequencing + `MainWindow.xaml.cs` (`actorId`, switch button, reconciliation) + `PosHostBuilder` DI — depends on Units 1 and 2 | Single PR, commits 3.x | `dotnet test Commerce.sln` (full suite; no WPF-specific automated test exists) | Manual WPF walkthrough per the design's scripted scenarios | Revert; `actorId` returns to `_installationId` |

## Unit 1: Pure Local Code — PIN Verifier, Cached Operator, Local Store, Current Operator

- [x] 1.1 RED: `OperatorPinCredentialTests` — a correct PIN verifies against its own derived salt/subkey; a wrong PIN fails; two derivations of the same PIN produce different salts yet both verify correctly (random salt); `IsValidPin` accepts a 6-digit string and rejects 5/7-digit strings, non-digit input, all-same-digit (`111111`), and strictly ascending/descending runs (`123456`, `654321`) — spec: pos-operator-session "One-Time Online Provisioning Per Terminal-Operator Pair" (PIN policy check precedes any I/O).
- [x] 1.2 GREEN: create `src/Commerce.Pos.Windows/OperatorPinCredential.cs` — `IsValidPin`, `Derive(pin) -> (Salt, Subkey)` via `Rfc2898DeriveBytes.Pbkdf2` (128-bit salt, 256-bit subkey, 210 000 iterations, HMAC-SHA256), `Verify(pin, salt, subkey)` via `CryptographicOperations.FixedTimeEquals`. Pure, no I/O, no WPF reference.
- [x] 1.3 RED: `CachedOperatorTests` — `IsStale` is false at 13 days since `LastVerifiedUtc`, true at 15 days, and exercises the exact 14-day boundary — spec: pos-operator-session "Offline Credential Staleness".
- [x] 1.4 GREEN: add `src/Commerce.Pos.Windows/CachedOperator.cs` — `record CachedOperator(Guid UserId, string Email, Guid OrganizationId, byte[] Salt, byte[] Subkey, DateTimeOffset LastVerifiedUtc)` with `Ttl = TimeSpan.FromDays(14)` and `IsStale(DateTimeOffset now)`.
- [x] 1.5 RED: `LocalOperatorStoreTests` — round-trip persists and reloads one operator; multiple operators coexist in the same file; `Upsert` by `UserId` replaces the existing entry in place rather than duplicating; `Remove` deletes only the targeted entry; `TouchVerified` updates only that entry's `LastVerifiedUtc` — spec: pos-operator-session "Multiple Cached Operators Per Terminal" (second operator provisioning does not disturb the first).
- [x] 1.6 RED: extend `LocalOperatorStoreTests` — a tampered/corrupted ciphertext on one entry's protected verifier field makes only that entry silently absent from `Load()` (no throw, other entries unaffected); truncated or garbage JSON in the whole file yields an empty list (no throw); a missing file yields an empty list (no throw) — spec: pos-operator-session "Corrupted local operator file is treated as no credential" (mirrors `LocalInstallationStore`'s `TryDecryptPairing` contract exactly).
- [x] 1.7 GREEN: create `src/Commerce.Pos.Windows/LocalOperatorStore.cs` — `operators.json` beside `installation.json` in the same `dataDirectory`; plaintext envelope per entry with only the verifier blob (`salt‖subkey`) DPAPI-protected via `ProtectedData.Protect(..., optionalEntropy: null, DataProtectionScope.CurrentUser)`; `Load()`, `Upsert(CachedOperator)`, `Remove(Guid userId)`, `TouchVerified(Guid userId, DateTimeOffset now)`; tolerant decrypt matching `LocalInstallationStore`'s exact pattern, never throws.
- [x] 1.8 RED: `CurrentOperatorTests` — `ResolveActorId(fallback)` returns the fallback when no operator is set; after `Set(op)` it returns `op.UserId`; after `Clear()` it returns the fallback again — spec: pos-operator-session "Sale proceeds with no operator identified" / "Sale is attributed to the current operator when identified".
- [x] 1.9 GREEN: create `src/Commerce.Pos.Windows/CurrentOperator.cs` — singleton holding `CachedOperator? Value`; `Set(CachedOperator)`, `Clear()`, `Guid ResolveActorId(Guid installationIdFallback) => Value?.UserId ?? installationIdFallback` (total function, no branch at the call site).
- [x] 1.10 Confirm Unit 1 green: `dotnet test tests/Commerce.Integration --filter FullyQualifiedName~OperatorPinCredential|FullyQualifiedName~CachedOperator|FullyQualifiedName~LocalOperatorStore|FullyQualifiedName~CurrentOperator` before starting Unit 2.

## Unit 2: Server Routes — Operator Verification and Status (depends on Unit 1 green)

- [x] 2.1 RED: `OperatorProvisioningTests POST /device/operators/verify` — valid operator credentials return 200 with the operator's real `userId`, `email`, `organizationId` — spec: pos-operator-session "Self-provisioning succeeds online".
- [x] 2.2 RED: extend — an unknown email, a wrong password, and a revoked user all return an identical bare 401 (same body shape), exercising `DeviceEndpoints.DummyPasswordHash` timing parity on both the unknown-directory-entry and unknown-email paths, mirroring `/device/pair` verbatim — spec: design "reuses PostgresUserAccountStore... including the shared DummyPasswordHash timing-parity call."
- [x] 2.3 RED: extend — an operator whose `BranchScope` does not contain the terminal's own branch (read from the stored `device_credentials` row, not the request body) returns 403 with status string `"branch-not-in-scope"` — spec: design "asserts server-side that the operator's BranchScope contains this terminal's branch."
- [x] 2.4 RED: extend — no response from this endpoint ever carries a `Set-Cookie` header (no `SignInAsync`, no session created) — spec: pos-operator-session provisioning requirement ("no separate authorization step"; design: "calls no SignInAsync: no cookie, no session, no server-side state").
- [x] 2.5 RED: extend — a missing or revoked device bearer token returns 401 before any credential check runs (`.RequireAuthorization("DeviceBearer")`).
- [x] 2.6 GREEN: add `OperatorVerifyRequest(string Email, string Password)` / `OperatorVerifyResponse(string Status, Guid? UserId, string? Email, Guid? OrganizationId)` records and `POST /device/operators/verify` in `src/Commerce.Cloud.Api/Endpoints/Device.cs`, under `.RequireAuthorization("DeviceBearer")`, reusing `PostgresUserAccountStore.FindDirectoryEntryAsync` → `FindByEmailAsync` → `PasswordHasher<UserAccount>.VerifyHashedPassword` → `IsRevoked` → `LoadActorAsync` verbatim from `/device/pair`, org/branch sourced from the stored `device_credentials` row.
- [x] 2.7 RED: `OperatorProvisioningTests GET /device/operators/{userId}/status` — an active, non-revoked user whose `BranchScope` contains the terminal's branch returns 200 `{"status":"active"}` — spec: pos-operator-session "Reconnection resets the staleness window."
- [x] 2.8 RED: extend — a revoked user returns 200 `{"status":"inactive"}` (not an error status) — spec: pos-operator-session "Stale cached credential requires re-provisioning" (server-side revocation path).
- [x] 2.9 RED: extend — a user id belonging to another organization also returns `"inactive"`, never a 404 or any signal that would leak cross-tenant existence — spec: design threat matrix "the status route... answers 'inactive' (never 404) for a foreign-org user id, so it cannot be used to probe account existence across tenants."
- [x] 2.10 RED: extend — a missing or revoked device bearer token returns 401 on this route too.
- [x] 2.11 GREEN: add `OperatorStatusResponse(string Status)` record and `GET /device/operators/{userId}/status` in `Device.cs`, under `.RequireAuthorization("DeviceBearer")`, scoped to the terminal's own organization from the stored device row.
- [x] 2.12 GREEN: create `src/Commerce.Pos.Windows/OperatorProvisioningClient.cs` — typed `HttpClient` with `VerifyAsync(email, password, deviceToken)` and `GetStatusAsync(userId, deviceToken)`, following `DevicePairingClient`'s discriminated-outcome shape (no unit test needed beyond what Unit 3's manual walkthrough and Unit 2's server-side integration tests already cover — this class has no branching logic of its own beyond deserializing the responses above).
- [x] 2.13 Confirm Unit 2 green: `dotnet test tests/Commerce.Integration --filter FullyQualifiedName~OperatorProvisioning` before starting Unit 3.

## Unit 3: WPF Operator Login Flow and Sale Attribution (depends on Units 1 and 2 green)

- [x] 3.1 GREEN: register in `src/Commerce.Pos.Windows/PosHostBuilder.cs` — `AddSingleton(new LocalOperatorStore(Path.Combine(dataDirectory, "operators.json")))`, `AddSingleton<CurrentOperator>()`, `AddHttpClient<OperatorProvisioningClient>` on the same `cloudApiBaseUrl` already used for `DevicePairingClient`.
- [x] 3.2 Confirm DI wiring: run the existing `PosCompositionRootTests` (extend if needed to assert `LocalOperatorStore`, `CurrentOperator`, and `OperatorProvisioningClient` resolve as singletons/typed clients, following `Build_Resolves_MainWindowDependencies_AsSingletons`'s existing pattern) — this is the one Unit-3 slice that fits the repo's existing automated-test convention for composition-root wiring.
- [x] 3.3 Create `src/Commerce.Pos.Windows/OperatorLoginWindow.xaml(.cs)` — modal window, `PairingWindow`'s exact shape (ctor-injected `OperatorProvisioningClient` + `LocalOperatorStore`, `DialogResult`, `public CachedOperator? ActiveOperator { get; private set; }`); picker of non-stale cached entries + PIN box, auto-selected with no picker shown when exactly one entry is cached (mirrors `/device/pair`'s auto-select-single-branch convention); a collapsed "Add another operator" panel (email/password/PIN/confirm PIN) revealed on demand via `BranchSelectionPanel`'s reveal idiom, and revealed automatically with the picker hidden when zero entries are cached. **No automated test exists or is added for this file's code-behind** — per the design and this repo's verified convention (no existing test covers `PairingWindow.xaml.cs`'s window logic), this is manual-verification-only: scripted walkthrough covering (a) first-run online provisioning, (b) same operator logging in offline by PIN with network disconnected, (c) a second operator provisioning and both appearing in the picker.
- [x] 3.4 Modify `src/Commerce.Pos.Windows/App.xaml.cs` — show `OperatorLoginWindow` after pairing resolves; unlike `PairingWindow`, cancel/close does NOT call `Shutdown()` — an explicit "Continue without operator" button and cancel/close both mean `MainWindow` opens with `CurrentOperator.Value == null`. **Manual verification only** (same rationale as 3.3): scripted walkthrough confirming "Continue without operator" reaches `MainWindow` and a subsequent sale still commits, attributed to `_installationId`.
- [x] 3.5 Modify `src/Commerce.Pos.Windows/MainWindow.xaml.cs` — `CommitSaleButton_Click` changes its `actorId:` argument to exactly `_currentOperator.ResolveActorId(_installationId)`, no added `if`/null-check at the call site; add a "Switch operator" button beside the existing `RepairButton_Click`'s button, opening the same `OperatorLoginWindow` with `Owner = this` (`RepairButton_Click`'s shape verbatim: explicit action, never an interrupt, never a precondition of `CommitSaleButton_Click`); extend `RefreshIdentityText` (or equivalent identity-display method) to show the current operator's email when set. **Manual verification only** (same rationale as 3.3): scripted walkthrough confirming a completed sale records the operator's user id as `actorId` when an operator is logged in, and confirming the switch-operator button never blocks or gates the sale button.
- [x] 3.6 Modify `src/Commerce.Pos.Windows/MainWindow.xaml.cs` `SyncButton_Click` — add reconciliation as the FIRST step, before the existing `pending.Count == 0` early return: for each cached entry (including stale ones), call `GET /device/operators/{userId}/status`; `"active"` → `LocalOperatorStore.TouchVerified(userId, now)`; `"inactive"` → `LocalOperatorStore.Remove(userId)`; unreachable/401 → leave the entry untouched (the 14-day TTL still applies). **Manual verification only** for the button-click wiring itself; the underlying `LocalOperatorStore.TouchVerified`/`Remove` behavior is already covered by Unit 1's automated tests (1.5/1.6), and the `GET .../status` response contract is already covered by Unit 2's automated tests (2.7–2.10) — this task only wires the two together inside WPF code-behind with no new branching logic to unit-test in isolation.
- [x] 3.7 Manual WPF walkthrough (full scripted pass, per the design's Testing Strategy): first run provisions online; network is then disconnected and the same operator logs in by PIN alone with zero network calls; a second operator provisions and both appear in the non-stale picker; "Continue without operator" reaches `MainWindow` and the sale commits attributed to `_installationId`; a corrupted `operators.json` is treated as "no credential" and the app does not throw on startup; pressing Sync with an empty outbox still reconciles operator status before the early return.
- [x] 3.8 Confirm full suite green: `dotnet test Commerce.sln` (all of Units 1–3's automated tests plus the full existing suite, zero regressions) before considering the change complete.

## Final Verification

- [x] 4.1 Confirm `src/Commerce.BranchNode/BranchNodeService.cs` is byte-unchanged (regression guard — the proposal explicitly excludes any `Commerce.BranchNode` signature change; `actorId` stays a plain `Guid`).
- [x] 4.2 Confirm no permission check was added to `CommitSaleButton_Click` and `Permission.Seller` remains `ViewSales`-only (regression guard against the "No Permission Gating Introduced" requirement).
- [x] 4.3 Confirm device pairing behavior is unchanged: a lost/revoked device credential still does not block a local sale (spot-check against existing `pos-installation-identity` tests — no new failure mode introduced).
- [x] 4.4 Run `dotnet test Commerce.sln` one final time as the change-level acceptance gate (matches the proposal's Success Criteria: "`dotnet test Commerce.sln` passes").
